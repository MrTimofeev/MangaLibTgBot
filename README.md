# 📚 MangaBot — Telegram-бот для отслеживания новых глав манги

**MangaBot** — это удобный Telegram-бот, который позволяет пользователям искать мангу, подписываться на обновления и получать уведомления о выходе новых глав. Также доступен поиск через **Inline-режим** и функция «Случайная манга» 🎲.

---

## 🚀 Основные функции

- 🔍 Поиск манги по названию через **Inline-запросы**
- 📥 Подписка на мангу — бот будет присылать уведомления о новых главах
- 📋 Просмотр списка ваших подписок
- 🗑️ Отписка от манги или удаление всех подписок
- 🎲 Получение случайной манги из базы
- 💾 Локальная база данных SQLite с полной историей манги и глав
- 🛠️ Автоматический парсинг новых глав с внешнего источника через Selenium
- 📅 Планировщик задач (`APScheduler`) для регулярной проверки обновлений

---

## 📁 Структура проекта

```
MangaBot/
├── bot/                # Логика Telegram-бота
│   ├── config.py       # Конфигурация (токены, режим отладки и т.д.)
│   ├── handlers.py     # Обработчики команд и callback-ов
│   └── message.py      # Тексты сообщений (например, help)
├── database/           # Работа с базой данных
│   └── db.py           # Модели SQLAlchemy + асинхронные методы
├── parser/             # Парсер манги
│   └── manga_parser.py # Асинхронный парсинг через Selenium
├── .env.example        # Пример файла переменных окружения
├── .gitignore
├── all_manga_parser.py # Скрипт для первоначального парсинга всей манги
├── main.py             # Точка входа
└── requirements.txt    # Зависимости (см. ниже)
```

---

## ⚙️ Установка и запуск

### 1. Клонируйте репозиторий
```bash
git clone https://github.com/MrTimofeev/MangaBot.git
cd MangaBot
```

### 2. Создайте виртуальное окружение (рекомендуется)
```bash
python -m venv venv
source venv/bin/activate      # Linux/macOS
# или
venv\Scripts\activate         # Windows
```

### 3. Установите зависимости
Убедитесь, что у вас установлен **Python 3.9+**, затем выполните:
```bash
pip install -r requirements.txt
```

### 4. Настройте переменные окружения
Скопируйте `.env.example` в `.env`:
```bash
cp .env.example .env  # Linux/macOS
# или
copy .env.example .env  # Windows
```

Заполните `.env`:
```env
BOT_TOKEN1_TG_debug=ваш_токен_для_отладки
BOT_TOKEN1_TG_relese=ваш_продакшен_токен
ADMINS=123456789          # ID админов через запятую: 123,456
MAX_SUBSCRIPTIONS=10      # Макс. число подписок на пользователя
```

### 5. Установите ChromeDriver
Бот использует **Selenium** для парсинга, поэтому необходим **ChromeDriver**.

- Убедитесь, что у вас установлен **Google Chrome**.
- Скачайте **ChromeDriver**, соответствующий версии вашего браузера:  
  👉 [https://chromedriver.chromium.org](https://chromedriver.chromium.org)
- Укажите путь к драйверу в файле `MangaBot/parser/manga_parser.py`:
  ```python
  WINDOWS_PATH_DRIVER = "D:\\путь\\к\\chromedriver.exe"
  LINUX_PATH_DRIVER = "/usr/bin/chromedriver"
  ```

### 6. Запустите бота
```bash
python main.py
```

> 💡 При первом запуске создастся файл базы данных `manga_bot.db`.  
> Чтобы наполнить базу мангой, запустите отдельно:
> ```bash
> python all_manga_parser.py
> ```

---

## 🤖 Использование в Telegram

- `/start` — начать работу
- `/help` — справка по командам
- Введите `@ваш_бот_манга название` в любом чате — мгновенный поиск через **Inline**
- Кнопка **«Случайная манга»** — получите случайную запись из базы

---

## 🔒 Безопасность

- Файл `.env` исключён из Git — ваши токены в безопасности
- Ограничение на число подписок (`MAX_SUBSCRIPTIONS`)
- Поддержка отладочного и продакшен-режимов (`debug` в `config.py`)

---

## 📝 Советы для разработчиков

- Не коммитьте `.env`!
- Для деплоя (например, в Docker) убедитесь, что Chrome и ChromeDriver установлены в образе.
- База данных — `sqlite+aiosqlite:///./manga_bot.db` (хранится в корне проекта).

---

## 📄 Лицензия

Проект создан в образовательных целях. Используйте на свой страх и риск.

---

> 🌟 Наслаждайтесь чтением манги без пропущенных глав!  
> Нашли баг или хотите улучшить бота? — Присылайте Pull Request!

---