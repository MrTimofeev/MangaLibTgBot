# 📚 MangaBot — Telegram-бот для отслеживания новых глав манги

**MangaBot** — это удобный Telegram-бот, который позволяет пользователям искать мангу, подписываться на обновления и получать уведомления о выходе новых глав. Также доступен поиск через **Inline-режим** и функция «Случайная манга» 🎲.

---

## 🚀 Основные функции

- 🔍 Поиск манги по названию через **Inline-запросы**
- 📥 Подписка на мангу — бот будет присылать уведомления о новых главах
- 📋 Просмотр списка ваших подписок
- 🗑️ Отписка от манги или удаление всех подписок
- 🎲 Получение случайной манги из базы
- 💾 Локальная база данных SQLite с полной историей манги и глав
- 🛠️ Автоматический парсинг новых глав с внешнего источника через **Selenium**
- 📅 Планировщик задач (`APScheduler`) для регулярной проверки обновлений

---

## 📁 Структура проекта

```
MangaBot/
├── bot/                # Логика Telegram-бота
│   ├── config.py       # Конфигурация
│   ├── handlers.py     # Обработчики команд
│   └── message.py      # Тексты сообщений
├── database/           # Работа с базой данных
│   └── db.py           # ORM-модели и методы
├── parser/             # Парсер манги
│   └── manga_parser.py # Использует Selenium + пути из .env
├── .env.example        # Пример переменных окружения
├── .gitignore
├── all_manga_parser.py # Скрипт для полного парсинга
├── main.py             # Точка входа
└── requirements.txt    # Зависимости
```

---

## ⚙️ Установка и запуск

### 1. Клонируйте репозиторий
```bash
git clone https://github.com/MrTimofeev/MangaBot.git
cd MangaBot
```

### 2. Создайте виртуальное окружение
```bash
python -m venv venv
source venv/bin/activate      # Linux/macOS
venv\Scripts\activate         # Windows
```

### 3. Установите зависимости
```bash
pip install -r requirements.txt
```

### 4. Настройте `.env`-файл
Скопируйте шаблон и заполните своими данными:
```bash
cp .env.example .env      # Linux/macOS
copy .env.example .env    # Windows
```

Пример содержимого `.env`:
```env
# Токены Telegram-бота
BOT_TOKEN1_TG_debug=ваш_токен_для_отладки
BOT_TOKEN1_TG_relese=ваш_токен_для_продакшена

# Администраторы (ID через запятую, без пробелов)
ADMINS=123456789

# Макс. число подписок на пользователя
MAX_SUBSCRIPTIONS=10

# Пути к ChromeDriver (укажите актуальные для вашей системы)
WINDOWS_PATH_DRIVER=Для windows
LINUX_PATH_DRIVER=Для Linux
```

### 5. Установите Google Chrome и ChromeDriver
- Установите **Google Chrome** (если ещё не установлен).
- Скачайте **ChromeDriver**, совместимый с вашей версией Chrome:  
  👉 [https://chromedriver.chromium.org](https://chromedriver.chromium.org)
- Разместите `chromedriver` в указанной в `.env` директории.

### 6. Запустите бота
```bash
python main.py
```

> 🔧 При первом запуске создастся файл `manga_bot.db`.  
> Чтобы заполнить базу мангой, выполните:
> ```bash
> python all_manga_parser.py
> ```

---

## 🤖 Использование в Telegram

- `/start` — начать
- `/help` — справка
- `@ваш_бот название` — поиск манги в любом чате (Inline-режим)
- Кнопка **«Случайная манга»** — получить случайную запись

---

## 🔒 Безопасность и гибкость

- Все конфиденциальные данные и пути вынесены в `.env`
- `.env` исключён из Git — ваши данные в безопасности
- Поддержка разных окружений: Windows / Linux
- Ограничение числа подписок на пользователя

---

## 📄 Лицензия

Проект предназначен для личного/образовательного использования. Автор не несёт ответственности за использование сторонних API или нарушение правил сайтов при парсинге.

---

> 🌸 Следите за новыми главами без усилий!  
> Нашли ошибку или хотите добавить фичу? — Pull Request приветствуется!